source "https://rubygems.org"

# Pinned to the same fastlane release LoopWorkspace's browser build uses, so the TestFlight
# workflows run on a known-good version. See fastlane/BROWSER_BUILD.md.
gem "fastlane", "2.237.0"
